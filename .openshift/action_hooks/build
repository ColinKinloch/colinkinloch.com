echo Running bower install
(cd "${OPENSHIFT_REPO_DIR}"; node_modules/bower/bin/bower install > bower.log)
echo Running grunt
(cd "${OPENSHIFT_REPO_DIR}"; node_modules/grunt-cli/bin/grunt > grunt.log)
